<!-- index.nj -->
{% extends "templates/layout.nj" %}

{% block content %}
  <h3>Gallery</h3>
  <div class="gallery">
    {% for image in images %}
      <div class="gallery__item">
        <img src="{{image.src}}" alt="{{image.alt}}">
        <img src="{{image.src}}" alt="{{image.alt}}">
      </div>
    {% endfor %}
  </div>
  <h2>This is the index page</h2>
  {% include "partials/navigation.nj" %}
  {% import 'macros/nav-macro.nj' as nav %}
  {{nav.active('home')}}
{% endblock %}
